<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.koreait.www.repository.ReplyMapper">

    <insert id="insert">
        INSERT INTO reply (bno, reply, replyer)
        VALUES (#{bno}, #{reply}, #{replyer})
    </insert>

    <select id="read" resultType="com.koreait.www.domain.ReplyVO">
        SELECT * FROM reply WHERE rno = #{rno}
    </select>

    <delete id="delete">
        DELETE FROM reply WHERE rno = #{rno}
    </delete>

    <update id="update">
        UPDATE reply SET reply = #{reply}, updateDate = now()
        WHERE rno = #{rno}
    </update>

    <select id="getList" resultType="com.koreait.www.domain.ReplyVO">
        SELECT * FROM reply 
        WHERE bno = #{bno} 
        ORDER BY rno ASC
    </select>

</mapper>